import { Router, Request, Response } from "express";
import { statusCode } from "..";

const router = Router();

//user signup route
router.post("/sigup", (req: Request, res: Response) => {});

//user signin route
router.post("/sigin", (req: Request, res: Response) => {});

export default router;
